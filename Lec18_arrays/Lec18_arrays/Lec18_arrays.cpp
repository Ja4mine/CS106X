
#include <iostream>
using namespace std;


void note() {
	/*
	type name[length];
	// memory does not need to be freed , will be automatially released
	ex:
	int homework[7];


	type* name = new type[length];
	用指针指向数组，必须手动释放，否则会内存泄露
	ex:
	int* homeworks = new int[7];

	但两者获取元素的方式是一样的

	---------------------------------------------------
	如果在[]之后写（），则会把所有的元素默认设为0，但这样更慢
	如果不写，所有的值都没有被初始化，里面就是垃圾，随机值
	
	int* a =new int[3];
	cout<< a[0];//2387432324
	cout<< a[1];//234241236

	int* a2 = new int[3]();
	cout<< a[0];//0
	cout<< a[1];//0

	int a[4]{} // 0

	---------------------------------------------------




	*/


}




int main()
{
   
}
